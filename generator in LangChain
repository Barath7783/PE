!pip install -U langchain langchain-community transformers  
from langchain_core.prompts import PromptTemplate 
from langchain_community.llms import HuggingFacePipeline  
from transformers import pipeline 
 
hf_pipeline = pipeline( 
"text-generation", 
model="gpt2", 
max_new_tokens=200, 
temperature=0.7 
) 
llm = HuggingFacePipeline(pipeline=hf_pipeline) 

prompt = PromptTemplate.from_template( 
"""Write a well-structured and informative paragraph about {topic}.
Explain key benefits, 
applications, and future impact in 5–6
sentences."""
) 

chain = prompt | llm 

result = chain.invoke({"topic": "Artificial Intelligence in Education"}) 
print(result)
