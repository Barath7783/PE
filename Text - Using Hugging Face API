import os
from openai import OpenAI

os.environ["HF_TOKEN"] = "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" # Replace actual Hugging Face API token

client = OpenAI(
base_url="https://router.huggingface.co/v1",
api_key=os.environ["HF_TOKEN"],
)

completion = client.chat.completions.create(
model="moonshotai/Kimi-K2-Instruct-0905",
messages=[
{
"role": "user",
"content": """Write a simple, clear paragraph explaining the future of artificial
intelligence in education. Keep the explanation between 80–100 words. Use a positive and
futuristic tone. Present the paragraph in plain English suitable for college students. Use the
title of the paragraph in your sentence."""
}
],
)

print(completion.choices[0].message.content)
